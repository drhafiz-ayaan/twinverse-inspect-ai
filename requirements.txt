# TwinVerse Inspect AI — full development environment
#
# Convenience aggregate for a single local venv covering both the API service
# and model training. Deployment should install the component files directly:
# the backend container needs only backend/requirements.txt, which excludes the
# multi-GB CUDA stack.
#
# Install:
#   python3.12 -m venv .venv && source .venv/bin/activate
#   pip install -r requirements.txt
#
# Verify the GPU is visible before any training work (see README D-002):
#   python -c "import torch; print(torch.cuda.is_available(), torch.cuda.get_device_name(0))"
#   -> True NVIDIA GeForce RTX 3080 Laptop GPU

-r backend/requirements.txt
-r ml/requirements.txt
