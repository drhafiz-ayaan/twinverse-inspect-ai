# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.ruff_cache/

# Node
node_modules/
.next/
out/
.turbo/
npm-debug.log*

# Environment / secrets
.env
.env.*
!.env.example
*.pem
*.key

# ML — datasets and weights (see README: use Git LFS or GitHub Releases)
# Directory contents are excluded with /* rather than /, so the documentation
# inside can be un-ignored: a negation cannot escape an excluded directory.
ml/datasets/*
!ml/datasets/README.md
ml/weights/*
!ml/weights/.gitkeep
ml/runs/
runs/
*.pt
*.onnx
*.engine

# Media / uploads
# Anchored to the repo root. Unanchored, "uploads/" matches at any depth — it
# silently swallowed frontend/app/api/inspections/[id]/uploads/route.ts, which
# would have left the upload endpoint out of the repo and out of the image.
/uploads/
*.mp4
*.mov

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# Deliverables: keep generators and outputs, not the npm tree
deliverables/node_modules/
